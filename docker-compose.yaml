services:
  app:
    build:
      context: ./app
      dockerfile: Dockerfiles/Dockerfile.local
    command: ["yarn", "run", "dev"]
    ports:
      - "5173:5173"
    volumes:
      - "./app:/usr/src/app"
      - "/usr/src/app/node_modules"